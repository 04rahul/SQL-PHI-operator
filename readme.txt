(open db2_project) as the root folder
Install all the dependencies
streamlit run mf_queries\frontend.py
streamlit run emf_queries\frontend.py

NOTE: Don't delete _generated.py, if required for testing keep it empty.
